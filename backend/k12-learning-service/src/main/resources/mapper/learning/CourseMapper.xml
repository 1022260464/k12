<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.k12.platform.learning.mapper.CourseMapper">
    <select id="selectForUpdate" resultType="com.k12.platform.learning.model.Course">
        SELECT * FROM learning_course WHERE id = #{id} AND deleted = 0 FOR UPDATE
    </select>
    <!-- 固定排序和绑定参数，不接收前端 SQL 字段名；LOCATE 将 % 和 _ 作为普通文本。 -->
    <select id="search" resultType="com.k12.platform.learning.model.Course">
        SELECT * FROM learning_course
        WHERE deleted = 0
        <if test="teacherId == null">AND status = 1</if>
        <if test="keyword != null">AND LOCATE(#{keyword}, title) &gt; 0</if>
        <if test="subject != null">AND subject = #{subject}</if>
        <if test="gradeLevel != null">AND grade_level = #{gradeLevel}</if>
        <if test="teacherId != null">AND teacher_id = #{teacherId}</if>
        ORDER BY updated_time DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
