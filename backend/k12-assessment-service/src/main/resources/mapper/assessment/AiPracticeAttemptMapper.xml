<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.k12.platform.assessment.mapper.AiPracticeAttemptMapper">
    <select id="findByRunAndStudent" resultType="com.k12.platform.assessment.model.AiPracticeAttempt">
        SELECT * FROM assessment_ai_practice_attempt
        WHERE run_id = #{runId} AND student_user_id = #{studentUserId}
    </select>
    <select id="findByRunAndStudentForUpdate" resultType="com.k12.platform.assessment.model.AiPracticeAttempt">
        SELECT * FROM assessment_ai_practice_attempt
        WHERE run_id = #{runId} AND student_user_id = #{studentUserId}
        FOR UPDATE
    </select>
    <select id="findRecentByStudent" resultType="com.k12.platform.assessment.model.AiPracticeAttempt">
        SELECT * FROM assessment_ai_practice_attempt
        WHERE student_user_id = #{studentUserId}
        ORDER BY created_time DESC, id DESC
        LIMIT #{limit}
    </select>
    <select id="canViewerAccessStudent" resultType="int">
        SELECT CASE WHEN
            EXISTS (
                SELECT 1 FROM assessment_homework_recipient r
                JOIN assessment_homework h ON h.id = r.homework_id
                WHERE r.student_user_id = #{studentUserId}
                  AND h.created_by = #{viewerUserId}
            ) OR EXISTS (
                SELECT 1 FROM learning_course_enrollment e
                JOIN learning_course c ON c.id = e.course_id
                WHERE e.user_id = #{studentUserId}
                  AND c.teacher_id = #{viewerUserId}
                  AND e.status = 'ACTIVE'
            ) THEN 1 ELSE 0 END
    </select>
</mapper>
