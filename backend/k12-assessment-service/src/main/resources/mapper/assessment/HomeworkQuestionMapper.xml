<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.k12.platform.assessment.mapper.HomeworkQuestionMapper">
    <select id="findByHomeworkId" resultType="com.k12.platform.assessment.model.HomeworkQuestion">
        SELECT * FROM assessment_homework_question
        WHERE homework_id = #{homeworkId} AND deleted = 0
        ORDER BY sort_order, id
    </select>
    <select id="totalScore" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(score), 0) FROM assessment_homework_question
        WHERE homework_id = #{homeworkId} AND deleted = 0
    </select>
</mapper>
