# K12 前端接口接入与待开发清单

本文记录 `user-app` 和 `admin-app` 的真实后端接入情况。前端统一通过 Gateway 的 `http://localhost:8080` 访问接口，不直接连接 IAM、Learning、Agent、Assessment 等微服务端口。

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 页面已完成 | 页面、加载状态、空状态、错误状态和主要操作均已接入真实 API |
| API 已封装 | 前端 API 方法已存在，但完整业务页面或操作流程尚未实现 |
| 后端待开发 | OpenAPI 中没有对应接口，前端只保留明确的空状态或禁用入口 |

## 用户端

### 页面已完成

| 功能 | 主要接口 |
| --- | --- |
| 登录、学生注册 | `POST /api/v1/iam/auth/login`、`POST /api/v1/iam/auth/register` |
| 当前用户 | `GET /api/v1/iam/me` |
| 课程目录和课程详情 | `GET /api/v1/learning/courses`、`GET /api/v1/learning/courses/{id}` |
| 章节、报名和学习进度 | 课程章节、报名状态、报名/退出、进度查询与更新接口 |
| 作业列表和详情 | 作业分页、作业详情、题目列表接口 |
| 作业提交 | `POST /api/v1/assessments/homeworks/{id}/submissions` |
| 学情档案 | 学情档案查询和更新接口 |
| 学习结果 | `GET /api/v1/assessments/learning-results/me` |
| AI 悬浮助教 | `POST /api/v1/agents/study-plan/runs`，当前使用同步运行模式 |

### API 已封装，页面待继续完善

| 功能 | 当前情况 |
| --- | --- |
| AI 运行记录、详情、产物 | 已封装运行列表、详情、产物、取消和重试方法；用户端尚未提供独立的运行中心页面 |
| 我的作业提交详情 | 已封装本人提交和提交详情接口；当前任务页以作业浏览与文本提交为主 |
| 修改密码和令牌状态 | 已封装接口；账户安全设置页面尚未建立 |

### 后端待开发

| 功能 | 前端处理 |
| --- | --- |
| 流式 AI 对话和会话历史 | 当前 Agent 后端是一次运行一次结果，没有 SSE/WebSocket 和对话历史接口，因此悬浮窗使用同步请求，不伪造流式输出 |
| 个性化课程推荐 | 当前没有推荐聚合接口；首页只展示课程目录中的真实数据和本地插画 |
| 全站搜索 | 当前没有统一搜索接口；头部搜索只在已有页面内筛选，不跨服务伪造结果 |
| 消息通知 | 当前没有通知中心接口，不展示虚假消息数量 |
| 课程封面、附件上传 | 当前没有统一媒体上传接口，页面暂用项目内漫画插画 |

## 管理端

### 页面已完成

| 功能 | 主要接口 |
| --- | --- |
| 管理员登录和权限拦截 | IAM 登录接口，登录后检查 `ROLE_ADMIN` |
| 管理概览 | 聚合用户、课程、智能体、作业数量，并检查各服务健康接口 |
| 用户管理 | 用户查询、新增、编辑、删除、启用/禁用、重置密码 |
| 角色权限 | 角色查询、角色权限更新 |
| 课程管理 | 课程查询、新增、编辑、删除 |
| 课程章节管理 | 章节查询、详情、新增、编辑、删除 |
| 智能体管理 | 智能体查询、新增、编辑、删除和测试运行 |
| 智能体运行中心 | 运行列表、详情、产物、取消和重试 |
| 作业管理 | 作业查询、新增、编辑、删除、发布和关闭 |
| 作业工作台 | 启用学生列表、单人勾选、全体/当前筛选结果批量选择、失效账号清理与校验、题目 CRUD、提交详情、单题与整份批改、批改历史 |
| 审计日志 | 登录审计和操作审计分页查询 |

### API 已封装，页面待继续完善

| 功能 | 当前情况 |
| --- | --- |
| 暂无 | 当前 OpenAPI 中已有的管理端资源操作均已提供 API 封装和页面入口 |

管理端不填充模拟数据。接口不可用时页面显示错误提示，写操作只能由后端成功响应后显示成功状态。

管理端编辑用户、课程、智能体和作业时，会先调用详情接口再打开表单，避免列表接口裁剪字段后出现编辑回显不完整。创建作业时从真实课程列表中选择课程，不要求管理员手填课程 ID。

当前用户模型没有班级、年级组织或教学组字段，因此“群体选择”先提供“全部启用学生”和“全选当前搜索结果”。后续若需要按班级、年级或课程成员布置作业，后端应增加学生分组/课程成员查询接口，前端再增加对应群组筛选，不应通过前端猜测分组。

### 后端待开发

| 功能 | 前端处理 |
| --- | --- |
| 系统设置 | 导航保留空状态页，等待配置管理接口 |
| 通知中心 | 顶栏按钮禁用并标注“通知功能待开发” |
| 管理端统一统计报表 | 暂由概览页并行读取各资源列表；后续应增加专用统计接口，避免数据量增大后拉取完整列表 |
| 文件与资源审核 | 当前没有审核工作流接口，不提供假操作入口 |

## 鉴权与错误处理

- 两个应用都把 JWT 放在 `sessionStorage`，请求时统一附加 `Authorization: Bearer <accessToken>`。
- 后端返回 `401` 时会清除本地会话，用户需要重新登录。
- `403` 表示已经登录但缺少权限，不能通过前端隐藏按钮代替后端的 `@PreAuthorize` 校验。
- 页面必须显示加载、无数据和请求失败状态，不能用静态成功数据替代接口结果。

## 后续开发顺序

1. 用户端账户安全页和 AI 运行记录页。
2. 后端补充通知、全站搜索、推荐、统计聚合和流式对话接口后，再开放对应前端入口。

## 本地运行

```bash
cd qianduan
pnpm dev:user
pnpm dev:admin
```

- 用户端：`http://127.0.0.1:5173/`
- 管理端：`http://127.0.0.1:5174/`
- Gateway：`http://127.0.0.1:8080/`

联调前应启动 Gateway、IAM、Learning、Agent、Assessment 以及它们依赖的 MySQL、RabbitMQ 和 Python Worker。未启动某个服务时，对应页面会显示真实错误状态。
